# binaries
/bin/
/dist/
*.exe
*.test

# Go
/vendor/
*.out
coverage.txt

# IDE
.idea/
.vscode/
*.swp

# env / secrets
.env
*.env.local
/deploy/compose/.env

# Claude Code session state (per-machine)
/.claude/

# Ad-hoc curl payloads / scratch files left in deploy/compose during
# manual smoke testing. Real fixtures live in the test scripts themselves.
/deploy/compose/tmp-*.json
/deploy/compose/chat.json
/deploy/compose/embed.json
/deploy/compose/embed2.json
/deploy/compose/login.json
/deploy/compose/mem.json

# OS
.DS_Store
Thumbs.db

# web frontend (internal/webui/) — see internal/webui/.gitignore for the
# fine-grained rules; this is a backstop in case the subdir gitignore is missing.
internal/webui/node_modules/
