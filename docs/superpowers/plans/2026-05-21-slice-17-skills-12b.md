# Slice 17 — Skills 12b (Tenant Skills API) Implementation Plan

> **Goal:** DB skills、admin CRUD、Resolver 合并 DB+FS、Admin Web UI；E2E **49**。

**Design:** Slice 12 spec §12b + MVP-P1 spec § Slice 17

**Depends on:** Slice 12a（Registry/Injector/Composer）

---

## Task 1 — Migrations

- [ ] `skills` 表 + `tenant_profile_skills`（见 slice-12-design）
- [ ] Repo CRUD

## Task 2 — Admin API

- [ ] `POST/GET/PUT/DELETE /admin/skills` + `RequireAdmin`
- [ ] 绑定 `PUT /admin/profiles/:name/skills`

## Task 3 — Resolver

- [ ] DB enabled skills 合并进 `Resolve()`；优先级：Session > Profile > default
- [ ] 与 filesystem 重复 id：DB 覆盖或 warn（ADR 文档化）

## Task 4 — Web Admin

- [ ] `pages/SkillsAdmin.tsx` 或并入 Audit 区
- [ ] 启用/禁用 toggle

## Task 5 — E2E 49

- [ ] admin 创建 skill body 含 `E2E_TENANT_SKILL_V1`
- [ ] agent.run / WS → `skill.inject` + 模型侧 marker（沿用 12a mock 约定）

**非目标：** 12c ecosystem sync、skill.run 工具
